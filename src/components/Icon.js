import { h } from "preact";

const Icon = ({ ...props }) => {
  // const iconAsset = require(`src/assets/icons/${icon}.svg`).default;
  return (
    <div>
      <p>topkek</p>
    </div>
  );
};

export default Icon;
